## Vela Exchange Subgraphs


```
npm install -g @graphprotocol/graph-cli
```

```
cd vela-token
npm run codegen
graph auth --product hosted-service <ACCESS_TOKEN>
graph deploy --product hosted-service <GITHUB_USER>/<SUBGRAPH NAME>
```
